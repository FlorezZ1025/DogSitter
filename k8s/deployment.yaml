apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-dogsitter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-dogsitter
  template:
    metadata:
      labels:
        app: api-dogsitter
    spec:
      containers:
      - name: api-dogsitter
        image: 533267353226.dkr.ecr.us-east-2.amazonaws.com/api-dogsitter:latest
        ports:
        - containerPort: 8080
        env:
        - name: ConnectionStrings__db
          value: "Host=api-dogsitter-db.cpmeaeuyijk2.us-east-2.rds.amazonaws.com;Port=5432;Database=dogsitter;Username=postgres;Password=BRVj8gE7PvYPnX5;SSL Mode=Require;Trust Server Certificate=true"