apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-dogsitter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-dogsitter
  template:
    metadata:
      labels:
        app: api-dogsitter
    spec:
      containers:
        - name: api-dogsitter
          image: 533267353226.dkr.ecr.us-east-2.amazonaws.com/api-dogsitter:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
